//children = Special property that simply outputs whatever gets entered between the
//           opening and closing tag.

const aux = (props) => props.children;

/*
const aux = function(props){
  return props.children
}
*/

export default aux;